from django.apps import AppConfig

class SchooltimetableConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "schooltimetable"
    verbose_name = "الجداول الزمنية"
