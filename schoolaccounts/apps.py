from django.apps import AppConfig

class SchoolaccountsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "schoolaccounts"
    verbose_name = "المستخدمون والصلاحيات"
